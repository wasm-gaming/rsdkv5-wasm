# Dependencies
node_modules/

# Cache
__pycache__/

# Build workspace + output (TS build + WASM are built in CI and attached to Releases)
.tmp/
.peers/
dist/
*.tsbuildinfo

# Game data — never committed. Provided by the host at runtime.
*.rsdk

# OS / editor
.DS_Store
.vscode/
